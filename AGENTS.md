# AGENTS

この repo は `D:\AI\Game\touge-bike-battle-royale` の closed alpha 作業対象です。

- 作業ブランチは `codex/closed-alpha-release` を1本だけ使う。
- QCDSは成果物の存在だけで合格にしない。GitHub Pagesまたはローカルサーバーで起動できる静的Webゲームとして、非blank表示と主要操作を必須とする。
- QCDS grade は10段階スケール以外を禁止する。
- 手動テスト未実施のため、Qualityは `S-`、Satisfactionは `A-` を実務上限にする。
- 文字化けがある created_idea / ZIP / 既存docsの本文はコピーしない。
