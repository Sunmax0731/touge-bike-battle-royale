# AGENTS

この repo は `D:\AI\Game\touge-bike-battle-royale` を正式配置先とします。

## 作業ルール

- 変更前に `README.md`、`AGENTS.md`、`SKILL.md`、`docs/` を確認する。
- 作業 branch は `codex/closed-alpha-release` 1本だけを使う。
- `npm test` は安定した validation/web smoke/QCDS/docs ZIP を生成する。
- 生成物に実行時刻、ローカル絶対パス、ZIPサイズ、ランダム値を入れない。
- Markdown / JSON / JS / HTML / CSS / SVG は UTF-8 と LF を前提にする。
- 手動テスト未実施のまま S+ 評価を付けない。
