# 実装計画

1. product metadata と代表シナリオを正常な日本語で再構成する。
2. core evaluator、validator、CLI、Web UI を責務分割する。
3. GitHub Pagesまたはローカルサーバーで起動できる静的Webゲームとして、非blank表示と主要操作を必須とする。
4. QCDS grade scale と文字化け検査を自動化する。
5. docs ZIP と prerelease asset を更新する。
