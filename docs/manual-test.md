# 手動テスト

Codex側では手動テスト未実施です。

1. `npm test` を実行する。
2. `index.html` を開く。
3. `happy-path`、`missing-required`、`warning`、`mixed-batch` を順に選択する。
4. canvasと結果パネルが更新されることを確認する。
5. GitHub Pagesまたはローカルサーバーで起動できる静的Webゲームとして、非blank表示と主要操作を必須とする。
6. release asset 3件を確認する。
